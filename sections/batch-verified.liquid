<section class="batch-verified" data-batch-verified-section="{{ section.id }}">
  <div class="container">
    <div class="batch-verified-in">
      <div class="left">
        <div class="batch-v-img">
          {% if section.settings.image != blank %}
            {{
              section.settings.image
              | image_url: width: 700
              | image_tag: loading: 'lazy', alt: section.settings.image.alt, class: 'img'
            }}
          {% endif %}
        </div>
      </div>

      <div class="right">
        <div class="text-animation">
          {% if section.settings.heading != blank %}
            <h2>{{ section.settings.heading }}</h2>
          {% endif %}
          {% if section.settings.subheading != blank %}
            <p>{{ section.settings.subheading }}</p>
          {% endif %}
        </div>

        <div class="batch-varified-accordion">
          {% for block in section.blocks %}
            <div
              class="batch-varified-a-item{% if forloop.first %} active{% endif %}"
              {{ block.shopify_attributes }}
            >
              <button
                type="button"
                class="batch-varified-a-title"
                aria-expanded="{% if forloop.first %}true{% else %}false{% endif %}"
              >
                <span>{{ block.settings.title }}</span>
                <span class="batch-varified-a-icon" aria-hidden="true"></span>
              </button>

              <div class="batch-varified-a-panel">
                <div class="batch-varified-a-content">
                  {% if block.settings.description != blank %}
                    <p>{{ block.settings.description }}</p>
                  {% endif %}

                  {% if block.settings.report_text != blank %}
                    <a href="{{ block.settings.report_link | default: '#!' }}" class="primary-btn" target="_blank">
                      {{ block.settings.report_text }}
                    </a>
                  {% endif %}
                </div>
              </div>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>
    <div class="bvi-text">
        <p>{{ section.settings.testedby }}</p>
      </div>
  </div>
</section>

<style>
  .batch-verified {
    padding: 0;
  }

  .batch-verified .batch-verified-in {
    display: grid;
    grid-template-columns: 4fr 5fr;
    align-items: start;
    gap: clamp(48px, 5vw, 60px);
    overflow: hidden;
  }

  .batch-verified .left {
    position: relative;
    width: 100%;
    height: 100%;
  }

  .batch-verified .batch-v-img {
    filter: grayscale(1);
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }

  .batch-verified .right {
    padding-top: 0;
  }

  .batch-verified h2 {
    max-width: 16ch;
  }

  .batch-verified .batch-varified-accordion {
    border-top: 0;
    padding-top: 50px;
  }

  .batch-verified .batch-varified-a-item {
    border-bottom: 1px solid rgba(21, 21, 21, 0.18);
  }

  .batch-verified .batch-varified-a-title {
    width: 100%;
    min-height: 70px;
    padding: 0;
    border: 0;
    background: transparent;
    color: #000;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
    text-align: left;
    cursor: pointer;
    font-family: var(--tobias);
    font-size: 26px;
    font-weight: 400;
    line-height: 1.1;
    letter-spacing: 0;
  }

  .batch-verified .batch-varified-a-icon {
    position: relative;
    width: 22px;
    height: 22px;
    flex: 0 0 22px;
  }

  .batch-verified .batch-varified-a-icon::before,
  .batch-verified .batch-varified-a-icon::after {
    content: '';
    position: absolute;
    top: 50%;
    left: 50%;
    width: 22px;
    height: 1px;
    background: currentColor;
    transform: translate(-50%, -50%);
    transition: transform 0.32s ease, opacity 0.32s ease;
  }

  .batch-verified .batch-varified-a-icon::after {
    transform: translate(-50%, -50%) rotate(90deg);
  }

  .batch-verified .batch-varified-a-item.active .batch-varified-a-icon::after {
    opacity: 0;
    transform: translate(-50%, -50%) rotate(0deg);
  }

  .batch-verified .batch-varified-a-panel {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.5s cubic-bezier(0.4, 0, 0.2, 1);
    will-change: max-height;
  }

  .batch-verified .batch-varified-a-content {
    padding: 0 54px 20px 0;
  }

  .batch-verified .batch-varified-a-content p {
    margin: 0 0 20px;
    color: #5d5d5d;
    font-family: var(--aspekta);
    font-size: 14px;
    font-weight: 450;
    line-height: 112%;
    letter-spacing: 0;
  }

  .batch-verified .batch-varified-a-content .primary-btn {
    min-width: 158px;
    padding: 11px 26px;
  }

  @media (max-width: 991px) {
    .batch-verified-in .left {
      display: none;
    }

    .batch-verified .batch-verified-in {
      grid-template-columns: 1fr;
    }
    .batch-verified .batch-varified-accordion {
      padding-top: 30px;
    }
  }

  @media (max-width: 767px) {
    .batch-verified .batch-varified-a-title {
      min-height: 50px;
      font-size: 20px;
    }

    .batch-verified .batch-varified-a-content {
      padding-right: 34px;
    }

    .batch-verified .batch-varified-a-icon {
      width: 18px;
      height: 18px;
    }

    .batch-verified .batch-varified-a-icon::before,
    .batch-verified .batch-varified-a-icon::after {
      width: 18px;
    }

    .batch-verified .batch-varified-accordion {
      padding-top: 20px;
    }

    .batch-verified .batch-varified-a-content .primary-btn {
      min-width: 120px;
      padding: 10px 26px;
    }
    .batch-verified .batch-varified-a-content p {
      font-size: 12px;
    }
  }
</style>

<script>
  (function () {
    var section = document.querySelector('[data-batch-verified-section="{{ section.id }}"]');
    if (!section || section.dataset.batchVerifiedReady === 'true') return;

    var items = Array.prototype.slice.call(section.querySelectorAll('.batch-varified-a-item'));
    section.dataset.batchVerifiedReady = 'true';

    function setPanelHeight(item, isOpen) {
      var panel = item.querySelector('.batch-varified-a-panel');
      if (!panel) return;

      panel.style.maxHeight = isOpen ? panel.scrollHeight + 'px' : '0px';
    }

    function setItemState(item, isOpen) {
      item.classList.toggle('active', isOpen);
      setPanelHeight(item, isOpen);

      var button = item.querySelector('.batch-varified-a-title');
      if (button) button.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
    }

    items.forEach(function (item) {
      setPanelHeight(item, item.classList.contains('active'));
    });

    items.forEach(function (item) {
      var button = item.querySelector('.batch-varified-a-title');
      if (!button) return;

      button.addEventListener('click', function () {
        var wasOpen = item.classList.contains('active');

        items.forEach(function (accordionItem) {
          setItemState(accordionItem, false);
        });

        if (!wasOpen) setItemState(item, true);
      });
    });

    window.addEventListener('resize', function () {
      items.forEach(function (item) {
        if (item.classList.contains('active')) setPanelHeight(item, true);
      });
    });
  })();
</script>

{% schema %}
{
  "name": "Batch Verified",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Every Batch Independently Verified"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Sub Heading"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "testedby",
      "label": "Tested by para"
    },
  ],
  "blocks": [
    {
      "type": "verification",
      "name": "Verification item",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Heavy Metals"
        },
        {
          "type": "textarea",
          "id": "description",
          "label": "Description",
          "default": "Mercury, Lead, Arsenic & Cadmium screened against FSSAI limits."
        },
        {
          "type": "text",
          "id": "report_text",
          "label": "Report button text",
          "default": "View Report"
        },
        {
          "type": "url",
          "id": "report_link",
          "label": "Report link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Batch Verified",
      "blocks": [
        {
          "type": "verification",
          "settings": {
            "title": "Heavy Metals",
            "description": "Mercury, Lead, Arsenic & Cadmium screened against FSSAI limits.",
            "report_text": "View Report"
          }
        },
        {
          "type": "verification",
          "settings": {
            "title": "Pesticides",
            "description": "Each batch is reviewed for pesticide residue against applicable safety standards.",
            "report_text": "View Report"
          }
        },
        {
          "type": "verification",
          "settings": {
            "title": "Microbial Safety",
            "description": "Screened for microbial quality so the final formula stays clean and consistent.",
            "report_text": "View Report"
          }
        },
        {
          "type": "verification",
          "settings": {
            "title": "Aflatoxins",
            "description": "Tested for naturally occurring aflatoxins before release.",
            "report_text": "View Report"
          }
        },
        {
          "type": "verification",
          "settings": {
            "title": "Melamine",
            "description": "Reviewed to confirm melamine remains within accepted safety limits.",
            "report_text": "View Report"
          }
        }
      ]
    }
  ]
}
{% endschema %}
