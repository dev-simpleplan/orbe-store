<section class="built-for-how">
  <div class="container-left">
    <h2>{{ section.settings.heading }}</h2>

    <div class="built-for-how-in">
      <div class="left">
        <ul>
          {% for block in section.blocks %}
            <li {{ block.shopify_attributes }}>{{ block.settings.text }}</li>
          {% endfor %}
        </ul>
      </div>

      <div class="right">
        {% if section.settings.image != blank %}
          <img src="{{ section.settings.image | image_url: width: 912 }}" alt="{{ section.settings.image.alt | escape }}" class="img" width="912" height="580">
        {% else %}
          <img src="https://cdn.shopify.com/s/files/1/0739/8014/9916/files/Rectangle_3468604.png?v=1780383097" alt="Image" class="img" width="912" height="580">
        {% endif %}
      </div>
    </div>
  </div>
</section>

<script>
  (function () {
    function init() {
      const script = document.currentScript;
      const section = script ? script.previousElementSibling : document.querySelector('.built-for-how');
      if (!section) return;

      const items = section.querySelectorAll('li');
      if (!items.length) return;

      let ticking = false;

      function clamp(value, min, max) {
        return Math.min(Math.max(value, min), max);
      }

      function updateTextColor() {
        ticking = false;

        const rect = section.getBoundingClientRect();
        const distance = Math.max(1, window.innerHeight + rect.height);
        const progress = clamp((window.innerHeight - rect.top) / distance, 0, 1);

        items.forEach(function (item, index) {
          const itemProgress = clamp((progress - index * 0.18) / 0.34, 0, 1);
          const color = Math.round(140 - itemProgress * 140);
          item.style.color = 'rgb(' + color + ', ' + color + ', ' + color + ')';
        });
      }

      function requestUpdate() {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(updateTextColor);
      }

      updateTextColor();

      if (window.lenis && typeof window.lenis.on === 'function') {
        window.lenis.on('scroll', requestUpdate);
      }

      window.addEventListener('scroll', requestUpdate, { passive: true });
      window.addEventListener('resize', requestUpdate);
      window.addEventListener('load', requestUpdate, { once: true });
    }

    init();
  })();
</script>

{% schema %}
{
  "name": "Built For How",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Built For How Your Body Works"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    }
  ],
  "blocks": [
    {
      "type": "item",
      "name": "List Item",
      "settings": [
        {
          "type": "text",
          "id": "text",
          "label": "Text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Built For How",
      "blocks": [
        { "type": "item", "settings": { "text": "Uses a pill-in-pill design to separate oil-based and mineral nutrients" } },
        { "type": "item", "settings": { "text": "Prevents nutrients from interfering with each other before absorption." } },
        { "type": "item", "settings": { "text": "Helps protect stability and potency of sensitive ingredients." } }
      ]
    }
  ]
}
{% endschema %}
